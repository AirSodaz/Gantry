$schema: 'https://moonrepo.dev/schemas/project.json'

type: 'application'
language: 'other'
tags: ['infrastructure']

tasks:
  phase0-smoke:
    command: 'bash phase0-smoke.sh'
    type: 'test'
    local: true
