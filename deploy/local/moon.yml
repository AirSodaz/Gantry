$schema: 'https://moonrepo.dev/schemas/project.json'

layer: 'application'
language: 'other'
tags: ['infrastructure']

tasks:
  phase0-smoke:
    command: 'bash phase0-smoke.sh'
    type: 'run'
    options:
      runInCI: false
