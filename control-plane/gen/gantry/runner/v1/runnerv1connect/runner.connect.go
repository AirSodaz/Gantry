// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: gantry/runner/v1/runner.proto

package runnerv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/AirSodaz/gantry/gen/gantry/runner/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// RunnerSessionName is the fully-qualified name of the RunnerSession service.
	RunnerSessionName = "gantry.runner.v1.RunnerSession"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// RunnerSessionSessionProcedure is the fully-qualified name of the RunnerSession's Session RPC.
	RunnerSessionSessionProcedure = "/gantry.runner.v1.RunnerSession/Session"
)

// RunnerSessionClient is a client for the gantry.runner.v1.RunnerSession service.
type RunnerSessionClient interface {
	// Session opens a bidirectional stream for runner communication.
	Session(context.Context) *connect.BidiStreamForClient[v1.RunnerMessage, v1.ControlPlaneMessage]
}

// NewRunnerSessionClient constructs a client for the gantry.runner.v1.RunnerSession service. By
// default, it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses,
// and sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the
// connect.WithGRPC() or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewRunnerSessionClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) RunnerSessionClient {
	baseURL = strings.TrimRight(baseURL, "/")
	runnerSessionMethods := v1.File_gantry_runner_v1_runner_proto.Services().ByName("RunnerSession").Methods()
	return &runnerSessionClient{
		session: connect.NewClient[v1.RunnerMessage, v1.ControlPlaneMessage](
			httpClient,
			baseURL+RunnerSessionSessionProcedure,
			connect.WithSchema(runnerSessionMethods.ByName("Session")),
			connect.WithClientOptions(opts...),
		),
	}
}

// runnerSessionClient implements RunnerSessionClient.
type runnerSessionClient struct {
	session *connect.Client[v1.RunnerMessage, v1.ControlPlaneMessage]
}

// Session calls gantry.runner.v1.RunnerSession.Session.
func (c *runnerSessionClient) Session(ctx context.Context) *connect.BidiStreamForClient[v1.RunnerMessage, v1.ControlPlaneMessage] {
	return c.session.CallBidiStream(ctx)
}

// RunnerSessionHandler is an implementation of the gantry.runner.v1.RunnerSession service.
type RunnerSessionHandler interface {
	// Session opens a bidirectional stream for runner communication.
	Session(context.Context, *connect.BidiStream[v1.RunnerMessage, v1.ControlPlaneMessage]) error
}

// NewRunnerSessionHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewRunnerSessionHandler(svc RunnerSessionHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	runnerSessionMethods := v1.File_gantry_runner_v1_runner_proto.Services().ByName("RunnerSession").Methods()
	runnerSessionSessionHandler := connect.NewBidiStreamHandler(
		RunnerSessionSessionProcedure,
		svc.Session,
		connect.WithSchema(runnerSessionMethods.ByName("Session")),
		connect.WithHandlerOptions(opts...),
	)
	return "/gantry.runner.v1.RunnerSession/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case RunnerSessionSessionProcedure:
			runnerSessionSessionHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedRunnerSessionHandler returns CodeUnimplemented from all methods.
type UnimplementedRunnerSessionHandler struct{}

func (UnimplementedRunnerSessionHandler) Session(context.Context, *connect.BidiStream[v1.RunnerMessage, v1.ControlPlaneMessage]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("gantry.runner.v1.RunnerSession.Session is not implemented"))
}
